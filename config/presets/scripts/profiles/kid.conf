# Batamiga profile: Kid-friendly — locked-down, content filtered
# Content filtering (hiding games flagged by scraper metadata) lands
# with Phase 27's full implementation; this profile already sets the
# lockdown + volume side of it.
batamiga.ambiance=sound
system.es.menu=bartop
GamelistViewStyle=grid
audio.volume=40
